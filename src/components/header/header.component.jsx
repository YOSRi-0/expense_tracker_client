import React from 'react';
import { HeaderContainer, LoginButton } from './header.styles';

const Header = () => {
  return (
    <HeaderContainer>
      <LoginButton>Login</LoginButton>
    </HeaderContainer>
  );
};

export default Header;
